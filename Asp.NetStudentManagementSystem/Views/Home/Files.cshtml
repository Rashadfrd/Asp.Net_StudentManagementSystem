@using Asp.NetStudentManagementSystem.Models

@{
    ViewData["Title"] = "Fayllar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model List<FileModel>


    <table class="subjects-table table table-striped table-bordered">
        <thead>
            <tr>
                <th>FileName</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.FileName</td>
                    <td><a class="admin-buttons" asp-controller="Home" asp-action="DownloadFile" asp-route-fileName="@item.FileName">Yüklə</a></td>
                </tr>
            }
        </tbody>
    </table>

